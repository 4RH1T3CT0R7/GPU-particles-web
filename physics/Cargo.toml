[workspace]
members = ["crates/xpbd-core", "crates/xpbd-wasm"]
resolver = "2"

[workspace.dependencies]
glam = { version = "0.29", features = ["bytemuck"] }
bytemuck = { version = "1", features = ["derive"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
