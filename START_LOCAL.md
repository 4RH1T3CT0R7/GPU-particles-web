# 🚀 Как запустить локально

## Быстрый старт

```bash
# 1. Перейдите в директорию проекта
cd ~/GPU-particles-web

# 2. Убедитесь, что у вас последняя версия
git pull origin claude/improve-lighting-raytracing-8ooFX

# 3. Запустите локальный сервер
python3 -m http.server 8000

# 4. Откройте в браузере
# http://localhost:8000
```

## Альтернативные серверы

### Node.js (если установлен)
```bash
npx http-server -p 8000
```

### PHP (если установлен)
```bash
php -S localhost:8000
```

### Live Server (VS Code)
1. Установите расширение "Live Server"
2. Правый клик на `index.html` → "Open with Live Server"

## Проверка версий

### WebGL2 версия
```
http://localhost:8000/index.html
```

### WebGPU версия (с ray tracing)
```
http://localhost:8000/index-webgpu.html
```

### Debug страница
```
http://localhost:8000/debug.html
```

## ✅ Что должно работать

После локального запуска:
- ✅ Частицы видны (не черный экран)
- ✅ Кнопки фигур: Куб, Сфера, Тор и т.д.
- ✅ PBR освещение с bloom
- ✅ Морфинг между фигурами
- ✅ Все контроли интерфейса

## 🐛 Если не работает

1. Откройте консоль браузера (F12)
2. Проверьте вкладку "Console" на ошибки
3. Проверьте вкладку "Network" - все файлы должны загружаться со статусом 200

## 📝 Примечание

Локальная версия всегда актуальна, без задержек GitHub Pages!
