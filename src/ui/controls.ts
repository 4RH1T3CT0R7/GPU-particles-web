/**
 * UI Controls — barrel re-export
 * All implementation has moved to ./controls/ sub-modules.
 */

export { initUIControls } from './controls/index.ts';
export type { UIControlsContext, UIControlsResult } from './controls/types.ts';
